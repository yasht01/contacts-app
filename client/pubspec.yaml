name: client
description: Contacts app frontend (Flutter)

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  dio: ^3.0.10 # For client to server comms
  faker: ^1.3.0 # generate fake names

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true